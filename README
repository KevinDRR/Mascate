#Dependecias
npm install -g pnpm

#Descargar todas las dependencias que el proyecto necesita
pnpm install

#Iniciar el proyecto
npm run dev

#Url de acceso
http://localhost:3000
-http://localhost:5500
-http://localhost:3060